import type {PrivateMarkets} from '@/packages/core/private-markets';
import type {ShowEpisode,ShowMemory} from '@/packages/core/show';
import type {CutawayState} from '@/packages/core/cutaway';
import type {BoardData} from '@/packages/core/board';
import {EMPTY_AUDIENCE} from '@/packages/core/board';
import type {AudienceState} from '@/packages/core/audience';
import {create} from 'zustand';import type {Snapshot,FloorEvent} from '@/packages/core/types';
export type VoiceActivity={deskId:string;text:string;kind:string;id:number};
type Store={privateMarkets:PrivateMarkets|null;setPrivateMarkets:(value:PrivateMarkets)=>void;show:ShowEpisode|null;showBeat:number;showHistory:ShowMemory[];spotlight:boolean;theater:boolean;setShow:(show:ShowEpisode|null,beat?:number)=>void;rememberShow:(item:ShowMemory)=>void;setSpotlight:(on:boolean)=>void;setTheater:(on:boolean)=>void;cutaway:CutawayState|null;principalNumber:number;setCutaway:(c:CutawayState|null)=>void;setPrincipalNumber:(n:number)=>void;board:BoardData|null;audience:AudienceState;setBoard:(b:BoardData)=>void;setAudience:(a:AudienceState)=>void;snapshot:Snapshot|null;selected:string|null;event:FloorEvent|null;connected:boolean;voices:VoiceActivity[];setSnapshot:(s:Snapshot)=>void;select:(id:string|null)=>void;setEvent:(e:FloorEvent)=>void;setConnected:(v:boolean)=>void;addVoice:(v:VoiceActivity)=>void;removeVoice:(desk:string)=>void;clearVoices:()=>void};
export const useFloor=create<Store>(set=>({privateMarkets:null,setPrivateMarkets:privateMarkets=>set({privateMarkets}),show:null,showBeat:0,showHistory:[],spotlight:true,theater:false,setShow:(show,showBeat=0)=>set({show,showBeat}),rememberShow:item=>set(s=>({showHistory:[item,...s.showHistory].slice(0,8)})),setSpotlight:spotlight=>set({spotlight}),setTheater:theater=>set({theater}),cutaway:null,principalNumber:1,setCutaway:cutaway=>set({cutaway}),setPrincipalNumber:principalNumber=>set({principalNumber}),board:null,audience:EMPTY_AUDIENCE,setBoard:board=>set({board}),setAudience:audience=>set({audience}),snapshot:null,selected:null,event:null,connected:false,voices:[],setSnapshot:s=>set({snapshot:s,connected:true}),select:selected=>set({selected}),setEvent:event=>set({event}),setConnected:connected=>set({connected}),addVoice:v=>set(s=>({voices:[...s.voices.filter(x=>x.deskId!==v.deskId),v]})),removeVoice:desk=>set(s=>({voices:s.voices.filter(x=>x.deskId!==desk)})),clearVoices:()=>set({voices:[]})}));
